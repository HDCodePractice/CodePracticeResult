function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  circle(245,175,15);
  // right tiny eye (black)
  circle(120,100,100);
  // left ear (black)
  circle(275,100,100);
  // right ear (black)
  circle(200,230,25);
  // nose (black)
  circle(150,175,50);
  // left big eye (black)
  circle(150,175,15);
  // left tiny eye (black)
  circle(245,175,50);
  // right big eye (black)
  fill(30,30,30);
  // fill black
  circle(200,200,200);
  // face (white)
  circle(150,175,25);
  // left small eye (white)
  circle(245,175,25);
  // right small eye (white)
  fill(250,250,250);
  // fill white
}
