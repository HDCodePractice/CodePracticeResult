import SwiftUI

struct ContentView: View {
    @State var align : [[Alignment]] = [[.center,.center,.center,.center,.center,.center],[.center,.center,.center,.center,.center,.center],[.center,.center,.center,.center,.center,.center]]
    @State var rotate = 0
    @State var totalPoints = 0
    @State var isRunning = false
    var body: some View {
        }
    }
}
