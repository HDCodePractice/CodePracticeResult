import SwiftUI

struct ContentView: View {
    var body: some View {
        enum  connect4 : String{
        case blue
        case red
        }
        switch self{
        case blue{
        return blue
        }
        case red{
        return red
        }
        }
        VStack {
            HStack{
                
            }
        }
    }
}
