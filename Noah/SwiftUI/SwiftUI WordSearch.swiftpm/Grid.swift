
import SwiftUI

struct Grid {
//    let gridSize = 7
//    @State var vm = ViewModel()
//    let gridSize = 7
//    var spawnBank = ViewModel().bank
//    var grid = ViewModel().grid
//    mutating func spawnWord() -> [[GridItem]]{
//        let chosenWord = spawnBank[0].randomElement() ?? "NULL"
//        let randomLocation = Int.random(in: 1...gridSize*gridSize)
//        let randomLocation = 1
//        let col = randomLocation % gridSize
//        let row = (randomLocation - col)/gridSize
//
//        let chosenDirection =
//            col == 0 && row == 0 ? Int.random(in: 3...5) :
//            col == 0 && row == gridSize ? Int.random(in: 1...3) :
//            col == gridSize && row == 0 ? Int.random(in: 5...7) :
//            col == gridSize && row == gridSize ? [7,8,1].randomElement()! :
//            col == 0 ? Int.random(in: 1...5) :
//            col == gridSize ? [5,6,7,8,1].randomElement()! :
//            row == 0 ? Int.random(in: 3...7) :
//            row == gridSize ? [7,8,1,2,3].randomElement()! :
//            Int.random(in: 1...8)
//        print(chosenDirection)
//        print(col)
//        print(row)
//        if let index = spawnBank[0].firstIndex(of: chosenWord) {
//            spawnBank.remove(at: index)
//        }
//        for r in 0 ... gridSize {
//            for c in 0 ... gridSize {
//                if r == row && c == col {
//                    if chosenDirection == 1 {
//                        for chr in 0 ... chosenWord.count - 1 {
////                            grid[row-chr][col-chr].text = Array(arrayLiteral: chosenWord)[chr]
//                            grid[0][0].text = Array(arrayLiteral: chosenWord)[chr]
//                        }
//                    } else if chosenDirection == 2 {
//
//                    }
//                }
//            }
//        }
//    }

}






