let switchCounter = numberOfSwitches
var gemCounter = 0
if gemCounter == switchCounter {
    moveForward()
}
    while gemCounter != switchCounter && {
        collectGem()
        moveForward()
        if isBlocked {
            turnRight()
        }
    }
    moveForward()
