import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Student : Juan Mendes")
            Image(student)
            Image(code)
            Text()
        }
    }
}
