while !isBlocked {
    if isOnGem {
        collectGem()
    }
    else if isOnClosedSwitch {
        toggleSwitch()
    }
    moveForward()
}
turnRight()
moveForward()
turnRight()
while !isBlocked {
    if isOnGem {
        collectGem()
    }
    else if isOnClosedSwitch {
        toggleSwitch()
    }
    moveForward()
}
turnLeft()
moveForward()
turnLeft()
while !isBlocked {
    if isOnGem {
        collectGem()
    }
    else if isOnClosedSwitch {
        toggleSwitch()
    }
    moveForward()
}
